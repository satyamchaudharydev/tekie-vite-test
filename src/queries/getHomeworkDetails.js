const getHomeworkDetails = async (getComponents) => {
    c
    await Promise.all(getComponents.map((component) => 
                                                fetchByComponents[component]()
                                                )).then((response) => {
                                                    // map the response and replace key with component name
                                                    return response
                                                        
                                                    
            }) 
        }