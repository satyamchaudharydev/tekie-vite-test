const INVALID_EMAIL_PASSWORD = 'Invalid email or password'
const INSUFFICIENT_PERMISSION_ERROR = 'Insufficient permission error'

const roles = {
  mentee: 'mentee'
}
export {
  INVALID_EMAIL_PASSWORD,
  INSUFFICIENT_PERMISSION_ERROR,
  roles
}
